/**
 * Re-export absorbed time utilities from the deep AppointmentSchedule domain module.
 */
export {
	addMinutesToTime,
	isTimeOverlapping,
	minutesToTime,
	timeToMinutes,
} from "../domain/appointment-schedule";
